{
  "name": "proto-game",
  "version": "1.0.0",
  "description": "js-based, event-driven, minimal game loop for the browser",
  "main": "index.js",
  "scripts": {
    "autobuild": "watchify ./test/test-index.js -o ./test/out/out.js --verbose",
    "build": "browserify ./test/test-index.js -o ./test/out/out.js"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "watchify": "^3.11.0"
  }
}
