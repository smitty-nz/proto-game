{
  "name": "proto-game",
  "version": "1.0.0",
  "description": "js-based, event-driven, minimal game loop for the browser",
  "main": "index.js",
  "scripts": {
    "autobuild": "watchify ./index.js -o ./test/out.js --verbose",
    "build": "browserify ./index.js -o ./test/out.js"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "watchify": "^3.11.0"
  }
}
