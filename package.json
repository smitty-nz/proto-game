{
  "name": "proto-game",
  "author" : "smitty-nz",
  "version": "1.1.2",
  "license": "MIT",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/smitty-nz/proto-game"
  },
  "description": "Tiny, event-driven game loop for the browser. Requires packaging via browserify or similar to use.",
  "keywords": [ "game", "browser", "minimal", "lightweight", "es6" ],
  "main": "index.js",
  "scripts": {
    "autobuild": "watchify ./test/test-index.js -o ./test/out/out.js --verbose",
    "build": "browserify ./test/test-index.js -o ./test/out/out.js"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "watchify": "^3.11.0"
  }
}
