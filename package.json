{
  "name": "proto-game",
  "author" : "smitty-nz",
  "version": "1.1.1",
  "description": "Tiny, event-driven game loop for the browser. Requires packaging via browserify or similar to use.",
  "keywords": [ "game", "browser", "minimal", "light", "es6" ],
  "main": "index.js",
  "scripts": {
    "autobuild": "watchify ./test/test-index.js -o ./test/out/out.js --verbose",
    "build": "browserify ./test/test-index.js -o ./test/out/out.js"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "watchify": "^3.11.0"
  }
}
